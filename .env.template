# KEY_ENCRYPTION_SECRET must be a high-entropy secret (minimum 32 characters).
# Recommended: use a 32-byte random hex string (64 hex chars).
#
# Generate a secret (macOS/Linux):
#   openssl rand -hex 32
#
# Or with Node.js:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
KEY_ENCRYPTION_SECRET=change-me-in-production

# Database
POSTGRES_URL=

# Optional
# Runtime
PORT=3000
HOST=127.0.0.1
SUPPRESS_LOGS=

# Chain configuration
# CHAIN_MODE can be 'simulated' (default) or 'sepolia'
CHAIN_MODE=sepolia
# Required when CHAIN_MODE=sepolia
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/{API_TOKEN}
